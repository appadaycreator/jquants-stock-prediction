# J-Quants株価予測システム デプロイ用必要パッケージ（超軽量版）
# デプロイ時に最小限のパッケージのみインストール

# データ処理（最小限）
pandas>=2.0.0
numpy>=1.24.0

# API通信
requests>=2.28.0

# 設定管理
python-dotenv>=1.0.0
pyyaml>=6.0.0

# 軽量APIサーバ
Flask>=3.0.0

# セキュリティ（最小限）
cryptography>=3.4.8
