# J-Quants株価予測システム デプロイ用必要パッケージ
# デプロイ時に最小限のパッケージのみインストール

# データ処理（軽量版）
pandas>=2.0.0
numpy>=1.24.0

# API通信
requests>=2.28.0

# 株価データ取得
yfinance>=0.2.0

# 機械学習（軽量版）
scikit-learn>=1.3.0
# xgboost>=2.0.0  # デプロイ時は除外（重い）
# tensorflow>=2.13.0  # デプロイ時は除外（重い）

# データ可視化（軽量版）
matplotlib>=3.6.0
# seaborn>=0.12.0  # デプロイ時は除外

# 設定管理
python-dotenv>=1.0.0
pyyaml>=6.0.0

# 自動化・スケジューリング
schedule>=1.2.0

# 軽量APIサーバ
Flask>=3.0.0

# セキュリティ
cryptography>=3.4.8

# 個別銘柄バックテストシステム用
scipy>=1.10.0
